<div class='toolbar'>
  <ul class='actions'>
    <li>
    
    </li>
  </ul>
  <br class='clear' />
</div>

<%= render :partial => 'admin/shared/order_tabs', :locals => {:current => "Payments"} %>

<table class="index">
  <tr>
    <th><%= "#{t('spree.date')}/#{t('spree.time')}" %></th>
    <th><%= t("amount") %></th>
    <th><%= t("type") %></th>
    <th></th>
  </tr>
  <% @payments.each do |payment| %>
    <tr>
      <td><%= payment.created_at.to_s(:date_time24) %></td>
      <td><%= number_to_currency(payment.amount) %></td>
      <td><%= link_to payment.class.to_s, admin_order_payment_url(@order, payment) %></td>
      <!-- TODO: don't assume credit card, make it possible to edit other kinds of payments -->
      <td>
        <%= link_to_with_icon 'edit', t('edit'), edit_admin_order_payment_url(@order, payment) %>
      </td>
    </tr>
  <% end %>
</table>
<%= button_link_to t('continue'), admin_orders_url %>